// This file only exists to generate doctest's main() - the macro below expands
// into a full main() that discovers and runs every TEST_CASE in every other
// test file linked into this binary. Nothing else should go in this file.
#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include "doctest/doctest.h"
